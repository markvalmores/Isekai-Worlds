rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read and write for all collections in this sandbox to sync across all users, profiles and creations
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
